function configureGUIForPC(handles)

warndlg({'configureGUIForPC()' ...
    'PC-Specific Configuration Is Not Yet Implemented' ...
    'Deal with the consequences of your decision to use Windows'});

return

% fieldNames = fields(handles);
% for iF = 1:length(fieldNames)
%     theHandle = getfield(handles,fieldNames{iF});
%     switch get(theHandle,'Type')
%         
%         case 
%             
%             handles = findobj(theHandle,'Type')...